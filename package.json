{
  "name": "michael-whittemore-portfolio",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "deploy": "npm run build && npx wrangler deploy --assets=./dist --compatibility-date=2025-07-26 --name=michael-whittemore-portfolio"
  },
  "devDependencies": {
    "purgecss": "^7.0.2",
    "typescript": "~5.8.3",
    "vite": "^7.0.4",
    "wrangler": "^4.26.0"
  }
}
